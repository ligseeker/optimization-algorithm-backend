# 10_测试修复与文档完善

## 1. 测试目标

阶段 10 聚焦：
- 权限与访问控制
- 工作空间与流程图结构化 CRUD
- YAML 导入导出校验
- Redis 缓存策略一致性
- 操作日志正确性

## 2. 自动化测试清单

### 2.1 权限与登录
- `AuthorizationGuardTest`
  - 未登录访问 `/api/workspaces` 返回未登录错误码
- `ResourceAccessServiceImplTest`
  - USER 访问自己的 workspace：通过
  - USER 访问他人 workspace：拒绝
  - USER 访问他人 graph：拒绝
  - ADMIN 访问任意 workspace：通过

### 2.2 工作空间
- `WorkspaceAppServiceImplTest`
  - USER 分页仅看自己
  - ADMIN 可看全部
  - 重名创建冲突
- `WorkspaceAppServiceCrudTest`
  - 创建、修改、删除
  - 删除后查询报不存在

### 2.3 流程图结构 CRUD
- `NodePathConstraintCrudTest`
  - 创建/更新/删除节点
  - 删除节点联动删除路径与约束
  - 创建路径需校验节点存在
  - 结构写操作触发 `graph_version` 增量

### 2.4 YAML 导入导出
- `GraphYamlServiceImplTest`
  - 合法 YAML 导入成功
  - 重复节点 ID 导入失败
  - 路径引用不存在节点导入失败
  - 约束引用不存在节点导入失败
  - 导出内容由 MySQL 聚合生成

### 2.5 缓存策略
- `GraphDetailCacheStrategyTest`
  - 首次查询写入 `graph:detail:{graphId}:v{graphVersion}`
  - 二次查询命中缓存
  - 版本变化后读取新 key（v2）
- `OptimizeTaskStateServiceImplTest`
  - PENDING/RUNNING/SUCCESS/FAILED 状态更新同步缓存
- `OptimizeTaskAppServiceCacheTest`
  - 任务提交写 PENDING 缓存
  - 任务状态查询优先 Redis，未命中回源
  - 优化结果查询未命中回源并回填
  - retry 复用 taskId 时清理旧结果与旧状态缓存

### 2.6 操作日志
- `OperationLogAspectTest`
  - 成功接口写日志
  - 失败接口写日志
  - `requestParams` 不落敏感数据（当前为 null）

## 3. 需要人工验证的项

- 真正的 USER/ADMIN 端到端权限（基于真实数据库账号）
- 图编辑后，线上 Redis key 的版本切换与 TTL 行为
- 优化任务真实执行过程中的 RUNNING->SUCCESS/FAILED 状态链路
- OpenAPI 页面字段描述与示例展示效果

## 4. 修复项摘要

- 统一了查询 DTO 分页空值容错
- 增加 `graph_version` 字段与版本化聚合缓存
- 收敛任务状态更新入口，避免散落更新
- 增加操作日志注解与 AOP 记录
- 完善 OpenAPI 注解与 DTO 参数说明

## 5. 验证命令

```bash
mvn test
mvn -DskipTests package
```
