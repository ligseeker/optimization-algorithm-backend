// TODO: API_CONTRACT.md 当前没有 operation-log 白名单接口，暂保留最小占位类型。
// 后续如需接入，必须先补充真实控制器路径、DTO/VO 字段和响应结构。

export type OperationLogQueryRequest = Record<string, never>

export type OperationLogVO = Record<string, never>
